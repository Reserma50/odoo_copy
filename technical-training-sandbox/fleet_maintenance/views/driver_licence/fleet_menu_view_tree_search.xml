<odoo>
    <data>    
    <record id="fleet_driver_view_search" model="ir.ui.view">
        <field name="name">fleet.drivers.search</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <search string="Test Search">
                <field name="name"/> 
                <!-- if you comment the name field, this will not appear -->
                <!-- <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living area (sqm)" filter_domain="['|',('living_area', '=', self),('living_area', '>', self)]"/>
                <field name="facades"/> -->
                <separator/>
                <filter string="Conductores Reserma" name="state" domain="[('user_ids', '!=', False)]"/>
            

                <!--  <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                <group expand="1" string="Group By">
                    <filter string="Provider" name="provider" context="{'group_by':'delivery_type', 'residual_visible':True}"/>
                </group> -->

            </search>

        </field>
    </record>
    </data>
</odoo>           
